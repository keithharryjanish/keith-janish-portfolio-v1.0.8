continue where you left off and also the projects still do not look right look at i thing its the bottom scroll bar and the accent bar so change that please as well and each project has their own index.html is there a way to make it so it uses the [projectname].js file instead of using its own html for the indepth project stuff like the images and stuff used for the project since i did a test for adding a new project and duplicated operation station and it didnt transfer the images over and make sure that properly works as well i also just updated the keithjanish.jpg image as well